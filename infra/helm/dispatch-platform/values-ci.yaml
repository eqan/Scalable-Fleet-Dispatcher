# CI overlay for helm lint/template. Immutable tags only — never :latest.
imagePullPolicy: IfNotPresent

api:
  image:
    repository: dispatch-api
    tag: sha-ci

worker:
  image:
    repository: dispatch-worker
    tag: sha-ci

web:
  image:
    repository: dispatch-web
    tag: sha-ci
