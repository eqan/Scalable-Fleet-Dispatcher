# CI overlay for helm lint/template. Immutable tags only — never :latest.
imagePullPolicy: IfNotPresent

api:
  image:
    repository: arqh-api
    tag: sha-ci

worker:
  image:
    repository: arqh-worker
    tag: sha-ci

web:
  image:
    repository: arqh-web
    tag: sha-ci
